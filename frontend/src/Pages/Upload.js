import React from 'react';
import PdfUpload from '../Components/PdfUpload';

const Upload = () => {
    return (
        <div>
            <h2>Login Page</h2>
            <PdfUpload />
        </div>
    );
}

export default Upload;
